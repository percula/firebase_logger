name: firebase_logger
description: An extension for logger that logs to Firebase Crashlytics
version: 1.0.0

environment:
  sdk: '>=2.16.2 <3.0.0'


dependencies:
  logger: #'>1.1.0 <2.0.0'
    git: https://github.com/percula/logger.git
  firebase_crashlytics: '>=2.5.0 <3.0.0'

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
